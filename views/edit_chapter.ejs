<!DOCTYPE html>
<html>
<head>
  <title>ShareYourStory</title>
  <style>
    body { font-family: Arial, sans-serif; max-width: 400px; margin: 40px auto; background: #f9f9f9; }
    h1 { text-align: center; }
    .logo { text-align: center; font-size: 2.2em; margin: 20px 0 10px 0; font-weight: bold; letter-spacing: 2px; color: #007bff; }
    form { background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 2px 8px #eee; }
    input, textarea {
      width: 100%;
      margin-bottom: 10px;
      padding: 8px;
      border-radius: 4px;
      border: 1px solid #ccc;
      background: #f7faff;
      box-shadow: 0 2px 8px #007bff11, 0 1.5px 0 #fff inset;
      transition: box-shadow 0.2s, border 0.2s;
    }
    input:focus, textarea:focus {
      border: 1.5px solid #007bff;
      box-shadow: 0 4px 16px #007bff22, 0 1.5px 0 #fff inset;
      outline: none;
      background: #fff;
    }
    button { padding: 10px 20px; border: none; background: #007bff; color: #fff; border-radius: 4px; cursor: pointer; }
    button:hover { background: #0056b3; }
    .error { color: red; margin-bottom: 10px; }
    a { text-decoration: none; color: #007bff; }
  </style>
</head>
<body>
  <div class="aurora-bg">
    <div class="aurora-anim aurora1"></div>
    <div class="aurora-anim aurora2"></div>
    <div class="aurora-anim aurora3"></div>
    <div class="aurora-anim aurora4"></div>
  </div>
  <div class="logo">ShareYourStory</div>
  <h1>Edit Chapter</h1>
  <form method="POST" action="/story/<%= storyId %>/chapter/<%= chapterId %>/edit">
    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    <input name="title" placeholder="Chapter Title" required value="<%= chapter.title %>" />
    <textarea name="content" placeholder="Chapter Content" rows="5" required><%= chapter.content %></textarea>
    <button type="submit">Save Changes</button>
  </form>
  <p><a href="/story/<%= storyId %>">&larr; Back to story</a></p>
</body>
</html> 