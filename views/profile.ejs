<!DOCTYPE html>
<html>
<head>
  <title>ShareYourStory</title>
  <style>
    body { font-family: Arial, sans-serif; max-width: 600px; margin: 40px auto; background: #f9f9f9; }
    h1 { text-align: center; }
    .logo { text-align: center; font-size: 2.2em; margin: 20px 0 10px 0; font-weight: bold; letter-spacing: 2px; color: #007bff; }
    .info { background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 2px 8px #eee; margin-bottom: 20px; }
    .stories { margin-top: 20px; }
    a { text-decoration: none; color: #007bff; }
  </style>
</head>
<body>
  <div class="logo">ShareYourStory</div>
  <h1><%= user.username %>'s Profile</h1>
  <div class="info">
    <% if (isSelf) { %>
      <div style="color: #28a745;">This is your profile.</div>
      <div style="margin:10px 0;"><a href="/user/<%= user.username %>/edit" style="color:#007bff;">Edit your author name</a></div>
    <% } %>
    <div>Joined: <%= new Date(user.joinDate).toLocaleDateString() %></div>
    <div>Stories: <%= userStories.length %></div>
    <div>Total Story Views: <%= totalViews %></div>
  </div>
  <div class="stories">
    <h2>Stories by <%= user.username %></h2>
    <% if (userStories.length === 0) { %>
      <p>No stories yet.</p>
    <% } else { %>
      <ul>
        <% userStories.forEach(function(story, idx) { %>
          <li><a href="/story/<%= story._index %>"><%= story.title %></a> (<%= story.views || 0 %> views)</li>
        <% }); %>
      </ul>
    <% } %>
  </div>
  <p><a href="/">&larr; Back to home</a></p>
</body>
</html> 