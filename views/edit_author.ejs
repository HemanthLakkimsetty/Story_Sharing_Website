<!DOCTYPE html>
<html>
<head>
  <title>Edit Author Name - Share Ur Story</title>
  <style>
    body { font-family: Arial, sans-serif; max-width: 400px; margin: 40px auto; background: #f9f9f9; }
    h1 { text-align: center; }
    .logo { text-align: center; font-size: 2.2em; margin: 20px 0 10px 0; font-weight: bold; letter-spacing: 2px; color: #007bff; }
    form { background: #fff; padding: 20px; border-radius: 8px; box-shadow: 0 2px 8px #eee; }
    input { width: 100%; margin-bottom: 10px; padding: 8px; border-radius: 4px; border: 1px solid #ccc; }
    button { padding: 10px 20px; border: none; background: #007bff; color: #fff; border-radius: 4px; cursor: pointer; }
    button:hover { background: #0056b3; }
    .error { color: red; margin-bottom: 10px; }
    .success { color: green; margin-bottom: 10px; }
    a { text-decoration: none; color: #007bff; }
  </style>
</head>
<body>
  <div class="logo">Share Ur Story</div>
  <h1>Edit Author Name</h1>
  <form method="POST" action="">
    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    <% if (success) { %>
      <div class="success"><%= success %></div>
    <% } %>
    <input name="newUsername" placeholder="New Author Name" required value="<%= user.username %>" />
    <button type="submit">Save</button>
  </form>
  <p><a href="/user/<%= user.username %>">&larr; Back to profile</a></p>
</body>
</html> 